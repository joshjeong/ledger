.container
  .header
    .title-header
      Welcome, #{@user.name.capitalize}!
    #add-btn.fa.fa-plus
  .ledger-list
    = render partial: "form"

  .ledgers
    - @ledgers.reverse_each do |ledger|
      .item
        = ledger.item
        $#{ledger.price}0
      .note  
        = ledger.note
      .date
        = time_ago(ledger.created_at)

  %table{class: "ledger-table"}
    %tbody
      %tr{class: "top_row"}
        %td
          Date
        %td
          Item
        %td
          Price
        %td
          Note
        %td
          Payment Type
        / - @ledgers.first.attributes.each do |attr_name, value|
      - @ledgers.each do |ledger|
        %tr{class: "table-content"}
          %td= get_date(ledger)
          %td= ledger.item
          %td= ledger.price
          %td= ledger.note
          %td= ledger.payment_type

